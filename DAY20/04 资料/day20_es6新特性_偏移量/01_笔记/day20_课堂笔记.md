#### 昨日回顾

```
原型和this指向
	原型对象 
		本质
			就是构造函数的一个属性(可以看作静态成员),每个构造函数都有这个属性
		作用
			可以把一些公共的属性和方法,放在里面,将来创建对象的时候,可以节省空间
	
	对象的原型
		本质
			就是对象的一个属性(属性名__proto__),每一个创建出来的对象都有这个属性
		作用
			为对象查找方法提供了一个机制
			先从构造函数中查找,没有的话,通过对象的原型找到原型对象
			如果原型对象里面没有,通过原型对象的原型,找到上一层,一直找到object
	
	构造函数
		在原型对象和对象的原型里面都有一个属性(constructor属性),指向构造函数本身
		可以记录,该对象引用于哪个构造函数
		
	原型链
		每一个实例对象,都有一个属性__proto__,指向构造函数的原型对象,构造函数的原型对象也是			一个对象,也有__proto__属性,指向它的上一层,这样一层一层就形成了原型链
		
	this指向
		不管是构造函数,还是原型对象,它里面的this都指向当前创建出来的对象
		
函数进阶
	存在地方				调用					this指向
	命名函数			函数名调用					window
	匿名函数			 变量名					window
	构造函数			  new					   创建处理的实例对象
	字面量对象中方法      对象						对象
	事件处理函数           事件触发	 				事件源对象
	定时器里面函数       指定的时间调用				window
	立即执行函数 	 		定义完之后立即执行			 window
    
改变函数的this指向 
	call(调用函数对象,参数...)

高阶函数
	函数作为参数或者返回值的一种情况

闭包
	概念
		也是一个函数,有权访问另外一个函数作用域内的变量,这变量所在的函数叫做闭包
	作用
		延长变量的作用范围,不仅可以在里面调用,还可以在外面访问
	应用
		立即执行函数,立即执行函数里面的任意一个函数都可以使用立即执行函数里面定义的变量


```

#### let和const

```
概念
	let用于声明变量,const用于声明常量
	const 声明一个只读的常量，一旦声明，常量的值就不能改变。

区别
					var					let					const
	作用域			没有块级作用			有块级作用			有块级作用
	变量提升	     有					 没有					没有
	能否被修改       可以					可以				不可以(可以修改内容)	
    
应用场景
	建议优先级  const > let > var
		1,const在编程语言中,一般表示常量,这样阅读代码的人,就不会随意修改,防止误操作
		2,js引擎对const做了优化,执行效率比较高
	具体使用
		1,声明基本数据类型,如果确定变量后面会被修改,使用let
			如果确定不修改,使用const,如果不确定修改,可以先用const,后期再改
		2,声明对象类型,比如数组,对象,一般很少修改他们的地址,更多的是修改里面的内容
			所以首选还是const
```



#### 类和继承

```
概述
    在ES6中，class (类)作为对象的模板被引入，可以通过 class 关键字定义类。
    class 的本质是 function。
    它可以看作一个语法糖，让对象原型的写法更加清晰、更像面向对象编程的语法。
    
语法
	class 类名{
		constructor(参数1,参数2){
			this.属性名1 = 参数1
			this.属性名2 = 参数2
		}

		方法名(参数列表){
			方法体
		}
	}
	
继承
	class A extends B

super关键字
	代表父类,可以调用父类的方法(普通方法和构造函数)
	
静态成员
	用static修饰的变量,叫做静态属性,只能通过类名调用
	用static修饰的方法,叫做静态方法,只能通过类名调用
```



#### 解构赋值

```
概述
	解构赋值是对赋值运算符的扩展。
	他是一种针对数组或者对象进行模式匹配，然后对其中的变量进行赋值。
	在代码书写上简洁且易读，语义更加清晰明了；也方便了复杂对象中数据字段获取。
	
语法
	数组模式匹配
		const [变量列表] = 数组
	对象模式匹配
		const {变量列表} = 对象
```



#### 箭头函数

```
概念
	箭头函数提供了一种更加简洁的函数书写方式
语法
	最简单的语法
		参数 => 函数体
	完整的格式
		(参数列表) => {函数体}
```



#### 数组遍历

```
四种方式
	1,普通for循环:速度最快,可读性差
	2,forEach:速度较快,并且能够控制内容,可以拿到索引和元素来操作
	3,for...of 只能获取到元素,获取不了索引
	4,for...in 只能获取索引,获取不到元素,可以根据索引获取元素
	
遍历对象
	for...of  可以使用解构的方式获取元素里面的属性
	for...in  可以获取对象的属性名,通过属性名获取属性值
```



#### 元素偏移量 

```
概念
	offset:偏移量,使用offset相关属性可以动态获取改元素的位置(偏移),大小等
作用
	1,获取元素距离带有定位父元素的位置(不带单位)
	2,获取元素自身的大小(宽高,不带单位)
方法
	element.offsetParent	返回带有定位的父级元素,如果父级都没有定位则返回body
	element.offsetTop	返回元素相对定位父元素上方的偏移
	element.offsetLeft	返回元素相对定位父元素左边的偏移
	element.offsetWidth	返回元素的宽度，包括边框和内边距，但不包含外边距（不带单位）
	element.offsetHeight 返回元素的高度，包括边框和内边距，但不包含外边距（不带单位）
```



