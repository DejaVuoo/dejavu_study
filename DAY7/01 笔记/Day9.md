# 昨日回顾

```
f1ex3个属性
    f1ex-grow定义项目的放大比例默认是0 不放大
    f1ex-shrink定义项目的缩小比例默认是1 空间不足 缩小
    f1eX-basis定义项目所占据的主轴空间，默认是auto 也可以用来设置初始的宽度
f1ex复合属性默认取值就是 0 1 auto
	其他取值
        flex:none ==>  flex: 0 0 auto 不放大不缩小
        flex:auto ==>  f1eX: 1 1 auto 放大缩小    
      	f1eX:数字  ==>  f1ex:数字 10% 放大缩小，数字代表占剩余尺寸份数
		f1eX:像素值==>  f1ex: 1 1 像素值 放大缩小，像素值代表子项初始宽度

布局顺序
	整体顺序
		从上到下从左往右从外向里
	css书写
        布局属性
        盒子模型+背景
        文本内容属性
        点缀属性
    选择器
    	类选择器+后代选择器

 动态变量
		:root+var()       
        
       
        
```

