# day12

# 昨日回顾

```
运算符
    算术运算符
    自增自减运算符
    比较运算符
    逻辑运算符
    赋值运算符
    三元运算符
流程控制
    顺序结构
    选择结构
        if语句
        if-else语句
        if-else-if语句
        switch语句
        
        区别
			if语句主要用于范围的判断，switch语句主要用于等值判断
	循环结构
        for循环
            for(var i=1;i<=10;i++){
                console.log(i)
            }
         whi1e循环
               var i=1;
               whi1e(i<=10){
                   console.log(i)
                   i++
               }
			do-wh1Te值环
                var i=1;
                do{
                	console.log(i)
                	i++
                }whi1e(i<=10)
```



# 循环语句

```
区别
    for循环和whi1e
    	每次先判断循环条件，再执行循环体
    do-whi1e循环
    	每次先执行循环体再判断条件（不管条件是否成立，都会先执行一次循环体）
 
应用场景 
	循环次数确定，建议使用fo下循环，因为它的结构比较简单紧凑
	循环次数不确定，一般循环条件是确定的，建议使用whi1e循环

continue
	用于跳过循环中的一个迭代。（跳过当前循环，继续下一次循环）
break
	用于跳出循环。	(终止当前循环)
```



# 数组

```
概念
	存储数据的容器，方便管理数据（对象类型）
作用
	使用单独的变量名来存储一系列的值，并且可以用变量名来访问里面的任意一个值
	数组中的每个元素都有自己的的id,以便它可以很容易地被访问到（id一般称之为索引）

定义和初始化
	var arr = []
	var arr = [1,2,3]
增
	arr[新的索引]=值
删
	delete arr[要删除的索引]
改	
	arr[要修改的索引]=新值
查	
	取值 arr[索引]
    长度 arr.length
遍历
	普通for循环
求和
	遍历并累加
求最值
	遍历并比较
二维数组
	概念
    	本质还是一个数组，只不过是数组里面的元素都是一维数组
    取值
    	arr[i]取的是里面的一维数组
		arr[i][j]取的是里面的一维数组中的元素
    遍历
        双重for循环		
    
    
    
```



# 函数

```

```

